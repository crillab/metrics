name:
date:

setup:
  os:
  cpu:
  ram:
  timeout:
  memout:

experiment-wares:
  -

input-set:
  name:
  type:
  family:
  input-name:
  extensions:
    -
  path-list:
      -

source:
  path:
  format:
  hierarchy-depth:
  experiment-ware:
  parser:

data:
  mapping:
    experiment_ware:
    cpu_time:
    input:
  raw-data:
    - file:
      name:
      pattern:
      regex:
      group:
  data-files:
    -
