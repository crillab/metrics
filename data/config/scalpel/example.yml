name:
date:

setup:
  os:
  cpu:
  ram:
  timeout:
  memout:

experiment-wares:
  -

input-set:
  name:
  type:
  family:
  input-name:
  extensions:
    -
  path-list:
      -

source:
  path:
    -
  format:
  has-header:
  quote-char:
  separator:
  hierarchy-depth:
  experiment-ware:
  parser:

data:
  mapping:
    experiment_ware:
    cpu_time:
    input:
  file-name-meta:
    pattern:
    regex:
    experiment_ware:
    input:
  raw-data:
    - log-data:
      name:
      pattern:
      regex:
      group:
  data-files:
    -
